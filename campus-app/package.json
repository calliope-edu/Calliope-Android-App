{
  "name": "calliope-campus-android-wrapper",
  "version": "0.1.0",
  "private": true,
  "description": "Capacitor shell that loads calliope-campus from Cloudflare and routes BLE through Android's native BluetoothManager.",
  "scripts": {
    "sync": "cap sync android",
    "open": "cap open android",
    "run": "cap run android"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.3.4",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@capacitor-community/bluetooth-le": "^8.1.3",
    "@capacitor/android": "^8.3.4",
    "@capacitor/core": "^8.3.4"
  }
}
